# topology for the ECG accelerator CONFIG window (one line per layer, Conv1..4).
# fields: in_ch  cp_en  nb  base   (cp_en/nb/base = weight-RAM word base)
# PTB-XL C4 (1,4,4,8) — same channel shape as Chapman but nb[Conv3]=7 (vs 6),
# from re-calibrating power-of-2 scales on PTB-XL (qat_ptbxl.py). This is exactly
# why per-layer nb is runtime-configurable: one bitstream, two datasets.
# total weight words = base[3]+in_ch[3] = 9+8 = 17.
1 15 8 0
4 15 6 1
4 255 7 5
8 255 7 9
